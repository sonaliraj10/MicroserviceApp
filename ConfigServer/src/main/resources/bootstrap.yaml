server:
  port: 8083

spring:
  cloud:
    config:
      server:
        git:
          uri:
          skip-ssl-validation: true
          
management:
  endpoints:
    web:
      exposure:
        include: "*"            
        