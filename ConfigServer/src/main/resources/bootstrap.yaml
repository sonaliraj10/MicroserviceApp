server:
  port: 8083

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sonaliraj10/PropertiesFile.git
          skip-ssl-validation: true
          
management:
  endpoints:
    web:
      exposure:
        include: "*"            
        